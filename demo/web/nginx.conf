
server {
  listen 80 default_server;

  server_name {{env "MODE"}}.{{env "SERVICE"}}.{{env "WWW_DOMAIN"}};

  root /home/app/www;
  index index.html index.htm;

  location / {
    try_files $uri $uri/ /index.html;
  }

}

